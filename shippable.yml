jobs:
- name: u16cppall
 type: runSh
 steps:
   - TASK:
       runtime:
         options:
           imageName: "drydock/u16cppall"
           imageTag: master
       script:
          - gcc --version
          - autoconf --version
          - automake --version
          - ccache --version
